{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raise.local/schemas/v1/arcadia/epbs/epbs-architecture.schema.json",
  "title": "EPBS Architecture",
  "description": "Architecture de définition des produits finaux (EPBS).",
  "x_schemaVersion": "1.0.0",
  "x_label": {
    "fr": "Architecture EPBS",
    "en": "EPBS Architecture"
  },
  "x_description": {
    "fr": "Vue globale définissant la structure de rupture du produit final et la stratégie d'intégration (IVVQ).",
    "en": "Global view defining the End-Product Breakdown Structure and integration strategy (IVVQ)."
  },
  "type": "object",
  "unevaluatedProperties": false,
  "allOf": [
    {
      "$ref": "../../common/types/base.schema.json"
    },
    {
      "$ref": "../metamodel/metamodel.schema.json#/$defs/BaseProperties"
    }
  ],
  "properties": {
    "configurationItems": {
      "type": "array",
      "items": {
        "$ref": "../metamodel/metamodel.schema.json#/$defs/Ref"
      },
      "description": "Liste des articles de configuration racines."
    },
    "capabilities": {
      "type": "array",
      "items": {
        "$ref": "../metamodel/metamodel.schema.json#/$defs/Ref"
      },
      "description": "Capacités d'intégration et de validation (IVVQ)."
    },
    "realizedPhysicalArchitecture": {
      "$ref": "../metamodel/metamodel.schema.json#/$defs/Ref",
      "description": "Lien vers l'architecture physique réalisée."
    }
  },
  "required": ["name"]
}
